class Simulation {
    constructor(SHOW, RESULTION, CANVAS_WIDTH, CANVAS_HEIGHT, SKIPS, PAUSED) {
        this.SHOW = SHOW;
        this.RESULTION = RESULTION;
        this.CANVAS_WIDTH = CANVAS_WIDTH;
        this.CANVAS_HEIGHT = CANVAS_HEIGHT;
        this.SKIPS = SKIPS;
        this.PAUSED = PAUSED;
    }
}