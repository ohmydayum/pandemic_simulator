class Perimeter {
    constructor(x_left, x_right, y_top, y_bottom, ) {
        this.x_left = x_left;
        this.x_right = x_right;
        this.y_top = y_top;
        this.y_bottom = y_bottom;
    }
}