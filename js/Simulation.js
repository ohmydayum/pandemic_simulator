class Simulation {
    constructor(SHOW, EPOCH, CANVAS_WIDTH, CANVAS_HEIGHT, SKIPS, PAUSED) {
        this.SHOW = SHOW;
        this.EPOCH = EPOCH;
        this.CANVAS_WIDTH = CANVAS_WIDTH;
        this.CANVAS_HEIGHT = CANVAS_HEIGHT;
        this.SKIPS = SKIPS;
        this.PAUSED = PAUSED;
    }
}